# --- Gradle / Kotlin -------------------------------------------------------
.gradle/
build/
**/build/
.kotlin/
# Secrets and machine-specific config (holds sdk.dir + github_token)
local.properties
secrets.properties

# --- Android ---------------------------------------------------------------
*.apk
*.aab
*.keystore
captures/

# --- Xcode / iOS -----------------------------------------------------------
xcuserdata/
**/xcuserdata/
*.xcuserstate
DerivedData/
**/DerivedData/
.swiftpm/
*.hmap
*.ipa
*.dSYM.zip

# --- Kotlin/Native ---------------------------------------------------------
*.klib

# --- IDEs ------------------------------------------------------------------
.idea/
*.iml
.fleet/
.vscode/

# --- Node / Vercel (docs site) ---------------------------------------------
node_modules/
.vercel/

# --- macOS -----------------------------------------------------------------
.DS_Store

# Downloaded Meta sample, kept locally for reference only
meta-wearables-dat-ios-main/

# Local credential backup — never commit
.credentials-backup/

# Release tooling — local only, not part of the published source
publish.sh

# iOS signing/Meta secrets — never commit
demo/iosApp/Configuration/Secrets.xcconfig
